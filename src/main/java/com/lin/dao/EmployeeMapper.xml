<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lin.dao.EmployeeMapper">

    <insert id="add" parameterType="Employee">
        insert into employee (lastName,email,gender,departmentId,birth)
        values (#{lastName},#{email},#{gender},#{departmentId},#{birth});
    </insert>

    <insert id="upd" parameterType="Employee">
        update employee
        set lastName=#{lastName},email=#{email},gender=#{gender},departmentId=#{departmentId},birth=#{birth}
        where id=${id};
    </insert>


    <resultMap id="EmployeeMap" type="Employee">
        <id property="id" column="eid"/>
        <result property="lastName" column="lastName"/>
        <result property="email" column="email"/>
        <result property="gender" column="gender"/>
        <result property="birth" column="birth"/>
        <association property="department"  javaType="Department">
            <id property="id" column="did"/>
            <result property="departmentName" column="dname"/>
        </association>
    </resultMap>
    <select id="getEmployees" resultMap="EmployeeMap">
        select e.id as eid,lastName,email,gender,birth,d.departmentId as did,d.departmentName as dname
        from department d,employee e
        where d.departmentId = e.departmentId
    </select>


    <select id="getEmployeeById" resultType="Employee" >
        select * from employee where id=#{id}
    </select>

    <delete id="deleteEmployee" parameterType="int">
        delete from employee where id=#{id};
    </delete>




</mapper>